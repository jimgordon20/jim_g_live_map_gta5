{
  "name": "jim_g_live_map_gta5",
  "version": "1.0.0",
  "private": true,
  "description": "GTA V Leaflet live map — GitHub Pages preview",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --outDir preview-dist"
  },
  "dependencies": {
    "gta-v-map": "^2.0.1",
    "leaflet": "^1.9.4"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
